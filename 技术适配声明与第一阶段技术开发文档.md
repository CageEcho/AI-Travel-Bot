# 技术适配声明与第一阶段技术开发文档
## 高端旅行智能方案生成平台「行策」

> **配套文档**：`PRD-v3-实施版.md`（实施规格）、`高端旅行智能方案生成平台-PRD-v2.md`（设计依据与决策理由）、《AI Agent 产品 Vibe Coding 通用技术栈手册 V2.1》
> **生成依据**：手册第五节流程（AI 读 PRD + 手册 → 输出技术适配声明 → 生成阶段文档 → 只开发当前阶段）
> **本文覆盖**：技术适配声明（全项目）+ 第一阶段 M0 技术开发文档

---

## ⚠️ 对 PRD-v3 的两处修正（对照手册后发现）

在生成本文档时，把 `PRD-v3-实施版.md` 逐条对照手册第九节工程底线，发现两处需要修正：

| # | PRD-v3 原文 | 问题 | 本文档采用 |
|---|---|---|---|
| **1** | §9「M0 用 `BackgroundTasks` + **内存字典**存状态」 | **违反手册 `[A]` 强制底线**："长时间任务必须保存可恢复的任务状态，不能只依赖内存变量" | 新增 `generation_task` 表持久化任务状态（见第六节） |
| **2** | §2.2 目录结构为 `app/{models,llm,retrieval,rules}` 扁平布局；Pydantic 模型放在 `models/` | 与手册第八节默认结构冲突：`models/` 是数据库模型，`schemas/` 是输入输出结构 | 采用手册默认结构，见第五节映射表 |

修正 1 是实质性的——生成耗时 30–60s，进程重启后任务状态丢失会导致前端永久轮询。
修正 2 是结构性的，早改成本低。

---

# 第一部分 · 技术适配声明

## 1. 产品形态判断

- **产品类型与核心任务**：面向高端旅行顾问的 AI 行程方案生成工作台（B 端内部工具）。顾问粘贴客户自然语言需求，系统通过多轮对话补齐关键条件，从结构化旅行知识库检索真实资源，生成可解释、带成本结构的按天行程；顾问审核确认后交付客户。
- **核心交互**：**多轮对话 + 分阶段确认**。链路为「需求采集 → 信息补全 → 知识检索 → 行程规划 → 产品推荐 → 人工确认」，其中需求卡确认、价格核对、可用性核对、对外发送四处为强制人工节点。
- **开发路径**：**纵向切片**。依据手册第四节判断规则——「多轮对话、人工确认、可视化编辑类产品：默认做最小纵向切片」。且本产品的三栏工作台（对话 / 需求卡 / 资源面板）**本身属于产品核心**（PRD-v2 决策 D1：让顾问看见 AI 的理解状态，是建立信任的机制），不是临时查看工具。
- **当前阶段范围**：M0 最小可跑闭环。目标是验证「粘贴一段客户原话 → 得到一份约束合法、成本正确、全部可溯源的 7 天行程」。

## 2. 采用的默认方案

| 方案 | 采用原因 |
|---|---|
| Python 3.11.x | 手册默认；Pydantic v2 + FastAPI 生态成熟 |
| FastAPI | 手册默认；自动 OpenAPI 便于无前端时用 Swagger 验证接口 |
| Pydantic v2 | 手册默认；本项目**一套模型三用**——Claude 结构化输出的 `output_format` + FastAPI response model + jsonb 字段校验器 |
| pytest | 手册默认；约束校验器的三态判定必须靠纯函数单测保证 |
| Git | 手册 `[A]` 强制底线 |
| 官方 SDK（`anthropic`） | 手册默认；使用 `client.messages.parse()` 获得结构化输出的原生校验 |
| SQLAlchemy + Alembic | 手册按需模块的配套默认；关系型数据库触发后默认采用 |
| 单 HTML 最小验收界面挂 `static/` | 手册第五节明确的默认实现——「新建后端且只需一次性验收工具时：单个 HTML 文件放在后端 `static/` 目录，由 FastAPI 直接挂载，原生 JS + fetch」。本项目已有可交互原型，直接复用 |
| `/api/v1` 前缀 | 手册 `[B]`；集中定义 |
| 提交 + 轮询状态 | 手册按需模块二选一中的一种（不用 SSE，理由见第 4 节暂缓 2） |

## 3. 触发的按需模块

| 模块 | 由哪条需求触发 | 当前阶段是否引入 |
|---|---|---|
| **关系型数据库** | 多实体关系（酒店 → 房型 → 价格档 → 供应商）、筛选统计（硬过滤检索是核心能力）、需求卡与方案的版本关系 | **引入**（但选型偏离默认，见第 4 节偏离 1） |
| SQLAlchemy + Alembic | 上一条触发后的配套 | 引入 |
| **持久化任务记录** | 单次方案生成耗时 30–60s（含回退重排），手册 `[A]` 要求可恢复 | **引入**（`generation_task` 表） |
| 向量检索 | 语义召回处理「安静有格调的温泉旅馆」这类模糊表达 | **不引入**，推到 M1（M0 硬过滤后候选仅 5–10 家，语义排序边际价值极低） |
| 最小可视化验收界面 | Agent 产出是「按天行程 + 成本结构」这类复杂结构化内容，靠 Swagger 的 JSON 无法判断质量；且需人工确认 | **引入**（复用已有原型） |
| 文件处理（docx / pdf / OCR） | 无此需求 | 不引入 |
| 对象存储 | M0 无大二进制资产 | 不引入 |
| 任务队列 | M0 单用户、并发 1–2 | 不引入（见第 4 节暂缓 3） |
| SSE | 见第 4 节暂缓 2 | 不引入 |

## 4. 偏离或暂缓的默认方案

### 偏离 1 · 数据库用 PostgreSQL 16，不用默认的 SQLite

- **替代方案**：PostgreSQL 16（Docker Compose 本地起），不使用手册默认的 SQLite。
- **原因**：本产品的核心能力是**结构化硬过滤检索**（PRD-v2 决策 D5），SQL 直接依赖两类 PG 特性：
  1. **数组操作符** —— `tier = ANY(%(tiers)s)`、`NOT (blackout_dates && stay_dates)`、`closed_days`/`dietary_support`/`tags` 全部是数组字段；
  2. **JSONB** —— `bed_config`、`extra_bed`、需求卡 `slots`、方案 `structure`。

  SQLite 无原生数组类型，这些字段要退化为 JSON 文本 + 应用层过滤，**硬过滤就从「数据库一次查完」变成「全表拉出再筛」**，既慢又把约束逻辑散到应用层。更关键的是 M1 的混合检索要求「过滤后再语义排序一次 JOIN 查完」（决策 D7），SQLite + 独立向量库会退回到两库数据一致性问题——那正是设计阶段明确否决的方案。
- **影响**：需要 Docker（一条 `docker compose up -d`）。开发者本机多一个依赖。
- **未来重评估条件**：无。这是长期决策，不是过渡方案。若将来索引规模超 ~50 万 chunk，再评估把向量层拆到专用向量库，关系层仍留在 PG。

### 偏离 2 · 向量检索用 pgvector，不用默认的 Chroma

- **替代方案**：pgvector（M1 启用），不使用手册默认的 Chroma 持久化模式。
- **原因**：同偏离 1。检索结构是「硬过滤 → 候选集 → 在同一候选集上做向量与关键词双路召回」。用 Chroma 意味着要么把过滤条件同步进向量库（一致性噩梦），要么先向量召回再过滤（召回质量崩坏）。
- **影响**：M0 阶段无影响（不引入向量）。M1 时需装 pgvector 扩展（已用 `pgvector/pgvector:pg16` 镜像预置）。
- **未来重评估条件**：索引规模超 ~50 万 chunk，或需要跨库共享向量索引时。

### 暂缓 1 · Next.js 正式前端

- **替代方案**：M0 用单 HTML 文件（已有原型）挂 `app/static/`，FastAPI 直接托管。
- **原因**：三栏工作台交互**属于产品核心**，按手册应作为正式前端的第一条纵向切片。但 M0 的目标是验证核心链路是否成立（约束能否拦住违规、成本能否算对、防幻觉是否有效），此时投入前端脚手架会拖慢核心验证。已有原型已具备三栏布局、需求卡四态角标、ⓘ 溯源浮层，改成 `fetch` 接真接口即可。
- **影响**：M0 的界面不支持拖拽编辑与响应式；仅作验收使用。
- **未来重评估条件**：**M2 进入编辑能力时**（拖拽调序、锁定、版本 diff、增量重排）。这些交互用原生 JS 维护成本会急剧上升，届时提升为 Next.js + TypeScript + Tailwind 的正式纵向切片。

### 暂缓 2 · SSE 流式输出

- **替代方案**：提交任务 + 轮询状态 + 获取结果。
- **原因**：本产品的产出是**一份完整的结构化方案**（按天行程 + 成本 + 违规清单），不是流式文本。中途片段对顾问无价值——看到「Day 3 排了强罗花坛」但还不知道成本是否超支、约束是否违规，无法做任何判断。而且生成过程含**回退重排**（违规则重排，最多 3 轮），流式输出会把中间被推翻的版本推给用户，反而制造困惑。轮询更简单且天然可恢复。
- **影响**：前端需轮询（2s 间隔），生成过程用分阶段进度条（检索中 / 编排中 / 校验中 / 核算中）替代逐字流式。
- **未来重评估条件**：若产品明确需要「边生成边逐天展示」的体验。

### 暂缓 3 · 任务队列

- **替代方案**：FastAPI `BackgroundTasks` + `generation_task` 表持久化状态。
- **原因**：M0 单用户、并发 1–2 个方案生成。手册 `[A]` 的要求是「任务状态必须持久化可恢复」，不是「必须用队列」——DB 表满足这一点。
- **影响**：进程重启时进行中的任务会停在 `running`，需要一个启动时的补偿逻辑把超时任务标为 `failed`（本阶段已包含）。
- **未来重评估条件**：并发生成 > 5，或需要跨进程重试与优先级调度。

## 5. 强制底线检查

| 检查项 | 状态 | 说明 |
|---|---|---|
| **密钥与隐私** | 通过 | `.env` 入 `.gitignore`，只提交 `.env.example`。`ANTHROPIC_API_KEY` 仅后端读取。**客户 PII 不进模型上下文**——需求卡用客户代号，姓名/联系方式不传给模型（PRD-v2 §14.4）。日志与 trace 不记密钥、不记完整客户原话（只记 digest） |
| **数据与任务可恢复** | 通过（已修正） | 需求卡、方案版本、生成任务全部落库。`generation_task` 表带 `status` 枚举与 `heartbeat_at`，服务启动时把僵死任务标 `failed`。**这一项修正了 PRD-v3 的内存字典方案** |
| **输入输出校验** | 通过 | 全链路 Pydantic。LLM 输出走 `client.messages.parse()` 原生校验；再经**约束校验器三态判定**（PASS / FAIL / **UNKNOWN**）+ **引用校验**（`resource_id` 必须在本次候选池内）+ **事实一致性回查**（逐字段比对库值） |
| **错误与日志边界** | 通过 | 统一错误结构 `{"error": {"code", "message"}}`；不返回堆栈。`trace_log` 记录 step / 耗时 / token / 错误类型 |
| **测试与真实模型验收** | 计划见第十节 | 第一层 mock：种子陷阱 12 条 + 约束校验器 21 个单测（7 约束 × 三态）+ 成本手工比对 + 引用校验拦截测试。第二层真实冒烟：槽位抽取与行程编排两种模型契约各跑通一条真实链路，记录模型 / 耗时 / token / 成本 / 结构合规率 |

**高风险动作的确定性约束**（手册 `[A]`）：

| 动作 | 约束方式 |
|---|---|
| 价格与金额 | **模型输出 schema 中不存在金额字段**；全部由 `cost.py` 规则引擎从 `rate_plan` 计算。任何从 LLM 响应读金额的代码都是 bug |
| 约束校验 | 确定性 Python 代码，不经过模型 |
| 生成方案 | 需求卡未 confirm 时 `POST /plans` 返回 409（人工节点 1） |
| 导出 / 对外发送 | M0 不实现导出。M2 实现时，价格与可用性未确认则接口层拒绝 |

## 6. 需要产品经理决定的问题

1. **种子数据的目的地范围**是否确认为东京 / 京都 / 箱根三城？（影响种子数据生成量与 M0 工期。PRD-v2 决策 D3 的依据是这三城覆盖约 71% 询单量，需你确认这个判断成立）
2. **单次生成的模型成本上限**是多少？（当前粗估 $0.15–0.25/次。若需压到更低，可把槽位抽取降到 `effort: low`，但抽取准确率会下降）
3. **M0 是否需要多用户与权限**？（本文档按单用户设计，无登录。若 M0 就要多顾问并行使用，需追加用户表与净价字段的角色过滤——后者是 PRD-v2 的 G5 上线门槛之一）

---

# 第二部分 · 第一阶段技术开发文档｜M0 最小可跑闭环

> 配套文档：`PRD-v3-实施版.md`、《AI Agent 产品 Vibe Coding 通用技术栈手册 V2.1》。
> 本文档只覆盖 M0。开发时不得提前实现 M1（向量检索）与 M2（编辑能力）的功能。

## 一、阶段目标

### 交付范围

打通「粘贴客户原话 → 槽位抽取与追问 → 需求卡确认 → 硬过滤检索 → 行程编排 → 约束校验（含回退重排）→ 成本核算 → 方案可视化」完整链路。

### 阶段产物

1. 可运行的 FastAPI 后端 + PostgreSQL
2. 3 城种子数据集（含 12 类**故意埋入的约束陷阱**）
3. 接真接口的最小验收界面（改造现有原型）
4. 20 条评测用例与跑测脚本
5. 测试套件：种子陷阱校验、21 个约束单测、成本比对、引用校验拦截

### 验收标准（对齐 PRD-v3 §12.1 的 G1–G5）

| # | 门槛 | 可执行判定 |
|---|---|---|
| G1 | 硬约束零违反 | 20 条评测用例中 `blocking` violations 总数 = 0；硬约束陷阱族用例 100% 通过 |
| G2 | 事实无编造 | 每个非 `free_time` 条目都有有效 `resource_id`；引用校验拦截数记入 trace |
| G3 | 成本核算一致 | 3 份方案手工核算 vs `calc_cost()` **逐分一致** |
| G4 | 人工卡点有效 | 未 confirm 的 `card_id` 调 `POST /plans` 返回 409 |
| G5 | 空值不静默通过 | 7 个 UNKNOWN 单测全绿；`min_child_age IS NULL` + 有儿童 → 出现在待核实清单 |

### 明确不做（防范围蔓延）

- ❌ 向量检索、字段模板化文本、别名表、RRF 融合、rerank（**M1**）
- ❌ 锁定、增量重排、版本 diff、资源替换、降本路径（**M2**）
- ❌ 18 个全量槽位（M0 做 12 个）、8 条全量冲突规则（M0 做 3 条）
- ❌ 9 条软约束（M0 只做 7 条硬约束）
- ❌ 导出 PDF / Excel、客户只读方案页
- ❌ 汇率服务、儿童分段票价、包车超时费
- ❌ 多用户、登录、净价角色过滤
- ❌ 知识库维护后台（种子数据直接入库，不做 CRUD 界面）

### 本阶段打通的主链路片段

```
客户原话 → [抽取] → 需求卡 → [人工确认①] → [硬过滤检索] → [编排]
   → [引用校验] → [约束校验] → 违规则回退重排(≤3) → [成本核算] → 方案可视化
```

## 二、技术适配摘要

- **引用结论**：见本文第一部分《技术适配声明》。
- **本阶段采用默认方案**：Python 3.11.x、FastAPI、Pydantic v2、pytest、Git、`anthropic` 官方 SDK、SQLAlchemy + Alembic、`/api/v1` 前缀、单 HTML 挂 `static/`、提交+轮询。
- **本阶段启用按需模块**：关系型数据库（多实体关系 + 硬过滤筛选触发）、持久化任务记录（生成耗时 30–60s 触发）、最小可视化验收界面（复杂结构化产出 + 人工确认触发）。
- **本阶段偏离**：PostgreSQL 16 替代 SQLite（数组操作符 + JSONB + M1 同库检索）。
- **本阶段暂缓**：向量检索（M1）、Next.js（M2）、SSE、任务队列。
- **开发路径**：纵向切片（复用已有原型作为切片界面）。

## 三、技术栈与模型

### 组件清单

| 组件 | 版本 | 本阶段是否用 |
|---|---|---|
| Python | 3.11.x | ✅ |
| FastAPI | ≥0.115 | ✅ |
| Pydantic | ≥2.9 | ✅ |
| SQLAlchemy | ≥2.0 | ✅ |
| Alembic | ≥1.13 | ✅ |
| psycopg | ≥3.2（binary,pool） | ✅ |
| PostgreSQL | 16（`pgvector/pgvector:pg16` 镜像） | ✅（pgvector 扩展本阶段不启用） |
| anthropic | 1.x | ✅ |
| pytest | ≥8 | ✅ |
| PyYAML | — | ✅（评测用例） |
| Chroma / 向量库 | — | ❌ 不引入 |
| Celery / Redis | — | ❌ 不引入 |
| Next.js / Node | — | ❌ 不引入 |
| python-docx / pypdf / OCR | — | ❌ 不引入 |

### 模型选型

| 用途 | 厂商 | 模型 | 配置 |
|---|---|---|---|
| 槽位抽取 | Anthropic | `claude-opus-5` | `thinking={"type":"adaptive"}`，`max_tokens=8000`，system prompt 开缓存 |
| 行程编排 | Anthropic | `claude-opus-5` | `thinking={"type":"adaptive"}`，`output_config={"effort":"high"}`，`max_tokens=32000` |
| Embedding | — | — | **本阶段不用**。M1 选型标准：中日文混合 query 的 Recall@10 实测 |

**Opus 5 的三个硬性注意点**（写进 `core/llm.py` 注释，避免反复踩）：

1. `budget_tokens` 已移除 → 传了返回 400。控制深度用 `output_config.effort`。
2. `temperature` / `top_p` / `top_k` 已移除 → 传了返回 400。
3. 不支持 assistant prefill → 用结构化输出控制格式，不要用 prefill。

## 四、环境与配置

### `.env.example`（不含任何真实密钥）

```bash
# 模型
ANTHROPIC_API_KEY=              # 留空则由 `ant auth login` 的 profile 解析
CLAUDE_MODEL=claude-opus-5
PLAN_EFFORT=high

# 数据库
DATABASE_URL=postgresql+psycopg://postgres:dev@localhost:5432/travel

# 约束阈值（手册 [A]：不硬编码）
COMMUTE_MAX_MIN=180
COMMUTE_MAX_MIN_TRANSFER=300
PRICE_STALENESS_DAYS=90
RESOURCE_STALENESS_DAYS=180

# 编排
MAX_REPLAN_ROUNDS=3
COMPLETENESS_THRESHOLD=0.85
GENERATION_TIMEOUT_SEC=300

# 成本
SERVICE_FEE_RATE=0.08
FX_JPY_CNY=0.0479

# 排序权重（商业优先级上限锁死 0.10，见 core/config.py 的 validator）
W_TAG_MATCH=0.50
W_PRICE_FIT=0.30
W_FRESHNESS=0.20
W_COMMERCIAL=0.00

# 服务
API_PORT=8000
LOG_LEVEL=INFO
```

### 需要产品经理提供

| 项 | 何时需要 | 无此项时 |
|---|---|---|
| `ANTHROPIC_API_KEY` | **验收前** | 开发期用占位值 + mock 测试推进（手册第六节：没有 Key 不阻塞开发，只阻塞最终验收） |

### 启动与外部依赖

```bash
docker compose up -d                                    # PostgreSQL 16，占用 5432
python -m venv .venv && source .venv/bin/activate
pip install -r backend/requirements.txt
alembic upgrade head                                    # 建表
python seed/generate.py && python seed/load.py          # 生成并载入种子数据
uvicorn app.main:app --reload --port 8000               # 后端 + 验收界面
```

- 端口：**8000**（后端与验收界面同源，避免 CORS）、**5432**（PG）。冲突时改 `API_PORT` 并记录。
- 无其他外部服务依赖。

## 五、项目结构

采用手册第八节默认结构（**修正 PRD-v3 §2.2 的扁平布局**）：

```text
AI旅行项目/
├── docker-compose.yml
├── .env.example
├── .gitignore
├── README.md
├── backend/
│   ├── app/
│   │   ├── main.py                    # FastAPI 入口 + static 挂载
│   │   ├── api/
│   │   │   └── v1/
│   │   │       ├── conversations.py   # 会话与需求卡
│   │   │       ├── plans.py           # 生成、状态、取方案
│   │   │       ├── search.py          # 手动检索
│   │   │       └── trace.py           # 轨迹回放
│   │   ├── core/
│   │   │   ├── config.py              # Settings + 权重上限 validator
│   │   │   ├── db.py                  # 引擎与 session
│   │   │   ├── llm.py                 # anthropic 客户端封装
│   │   │   ├── errors.py              # 统一错误结构与异常处理器
│   │   │   └── logging.py             # trace_log 写入
│   │   ├── models/                    # SQLAlchemy 数据库模型
│   │   │   ├── resource.py            # hotel/room_type/rate_plan/vehicle/restaurant/poi/supplier
│   │   │   └── business.py            # conversation/message/requirement_card/plan_version/generation_task/trace_log
│   │   ├── schemas/                   # Pydantic 输入输出结构（一套三用）
│   │   │   ├── slots.py               # SlotValue/SlotSet/Followup/SlotExtraction
│   │   │   ├── plan.py                # PlannedItem/PlannedDay/ItineraryPlan/RenderedPlan
│   │   │   ├── search.py              # HotelQuery/HotelCandidate/SearchResult/RelaxationHint
│   │   │   ├── cost.py                # CostLine/CostSummary
│   │   │   └── common.py              # Violation/Verdict/ErrorBody/PlanStatus
│   │   ├── services/
│   │   │   ├── extract.py             # 槽位抽取
│   │   │   ├── planner.py             # 行程编排 + 回退重排编排逻辑
│   │   │   ├── retrieval.py           # 硬过滤检索 + 放宽建议
│   │   │   ├── render.py              # 事实回填 + 引用校验 + 一致性校验
│   │   │   ├── constraints.py         # 7 条硬约束（三态判定）
│   │   │   ├── conflicts.py           # 3 条冲突检测
│   │   │   ├── cost.py                # 成本规则引擎
│   │   │   ├── tasks.py               # generation_task 生命周期与启动补偿
│   │   │   └── prompts/               # 手册 [A]：Prompt 独立文件
│   │   │       ├── extract_slots.md
│   │   │       └── plan_itinerary.md
│   │   └── static/
│   │       └── index.html             # 最小验收界面（改造自原型）
│   ├── tests/
│   │   ├── test_seed_traps.py
│   │   ├── test_search_filter.py
│   │   ├── test_constraints.py        # 21 个：7 约束 × 三态
│   │   ├── test_cost.py
│   │   ├── test_render_guard.py
│   │   ├── test_conflicts.py
│   │   └── test_api_gate.py
│   ├── alembic/
│   └── requirements.txt
├── seed/
│   ├── generate.py                    # 生成 + assert_traps()
│   ├── load.py
│   └── data/*.json                    # 提交到 git
├── eval/
│   ├── cases/*.yaml                   # 20 条
│   └── run.py                         # 支持 --dry-run（跳过 LLM，只测规则层）
└── docs/                              # PRD、手册、本文档的副本
```

### 与 PRD-v3 的模块映射

| PRD-v3 位置 | 本文档位置 |
|---|---|
| `app/models/{slots,plan,resource}.py`（Pydantic） | `app/schemas/` |
| `app/llm/{client,extract,plan}.py` | `app/core/llm.py` + `app/services/{extract,planner}.py` |
| `app/retrieval/search.py` | `app/services/retrieval.py` |
| `app/rules/{constraints,conflicts,cost}.py` | `app/services/` 同名文件 |
| `app/render.py` | `app/services/render.py` |
| `app/config.py` / `db.py` | `app/core/` |
| `prototype/index.html` | `app/static/index.html` |

## 六、数据、资产与状态

### 持久化方式与原因

关系型数据库（PostgreSQL 16）。触发条件：多实体关系（酒店→房型→价格档→供应商）、硬过滤筛选统计是核心能力、需求卡与方案的版本关系。选型偏离理由见技术适配声明第 4 节。

**无大二进制资产**（M0 不含房型图、方案 PDF），不引入对象存储。

### 数据表

资源侧 7 张、业务侧 6 张。**完整 DDL 见 `PRD-v3-实施版.md` 第 3 章**，此处只列本文档新增与需要强调的部分。

#### 新增表：`generation_task`（修正 PRD-v3 的内存字典方案）

```sql
CREATE TABLE generation_task (
  task_id       TEXT PRIMARY KEY,
  plan_id       TEXT NOT NULL,
  card_id       TEXT NOT NULL REFERENCES requirement_card,
  status        TEXT NOT NULL,          -- queued|searching|planning|validating|costing|done|failed
  progress      NUMERIC(4,3) NOT NULL DEFAULT 0,
  replan_round  INT NOT NULL DEFAULT 0,
  error_code    TEXT,
  error_message TEXT,
  heartbeat_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  finished_at   TIMESTAMPTZ
);
CREATE INDEX ON generation_task (status, heartbeat_at);
```

**启动补偿逻辑**（`services/tasks.py`，FastAPI lifespan 启动时执行）：

```python
# 把僵死任务标为失败，避免前端永久轮询
UPDATE generation_task
SET status='failed', error_code='ORPHANED',
    error_message='服务重启导致任务中断，请重新生成',
    finished_at=now()
WHERE status NOT IN ('done','failed')
  AND heartbeat_at < now() - interval '%(timeout)s seconds';
```

生成过程中每进入一个阶段更新 `status` / `progress` / `heartbeat_at`。

#### 空值语义（本项目最关键的字段约定）

以下字段**允许 NULL，且 NULL 表示「未记录」，不等于「无限制」**：

| 表.字段 | NULL 的含义 | 校验器行为 |
|---|---|---|
| `room_type.min_child_age` | 最低入住年龄未记录 | 有儿童时判 `UNKNOWN`，进待核实清单 |
| `poi.closed_days` | 休馆日未记录 | 判 `UNKNOWN`（**不得视为全年无休**） |
| `poi.accessible` | 无障碍适配未记录 | 有无障碍需求时判 `UNKNOWN` |
| `restaurant.closed_days` | 定休日未记录 | 判 `UNKNOWN` |
| `restaurant.child_friendly` | 儿童政策未记录 | 有儿童时判 `UNKNOWN` |
| `vehicle.luggage_28` | 行李容量未记录 | 判 `UNKNOWN` |

> ⚠️ **这是 M0 最容易写错的地方。** `if room.min_child_age and room.min_child_age > age:`
> 这种写法会让 NULL 静默通过。必须显式三分支。测试用例见第十节。

### 状态机

| 状态机 | 状态值 | 转换规则 | 恢复方式 |
|---|---|---|---|
| **需求卡** | `draft → confirmed` | 完整度 ≥ 阈值才允许 confirm；confirm 后冻结版本，再改产生新版本 | 落库，按 `(conv_id, version)` 取 |
| **生成任务** | `queued → searching → planning → validating → costing → done` / `failed` | `validating` 发现 blocking 违规 → 回到 `planning`，`replan_round += 1`；超过 `MAX_REPLAN_ROUNDS` 则带违规进 `costing` 并在结果中标注 | 落库 + heartbeat + 启动补偿 |
| **方案版本** | `draft`（M0 只有这一个） | M2 才引入 `pending_review → reviewed → delivered` | 落库 |

**兼容策略**：状态值用字符串枚举，新增状态时旧数据不受影响。M0 不预先定义 M2 的审核状态（手册 `[A]`：不为了"永不改枚举"提前猜完未来需求）。

## 七、API / 工具设计

前缀 `/api/v1`，集中定义在 `app/api/v1/`。

### 统一错误结构（手册 `[A]`）

```json
{"error": {"code": "CARD_NOT_CONFIRMED", "message": "需求卡尚未确认，无法生成方案"}}
```

不返回堆栈。错误码表集中在 `core/errors.py`。

### 接口清单

| # | Method | Path | 请求 | 响应 | 错误码 |
|---|---|---|---|---|---|
| 1 | POST | `/api/v1/conversations` | — | `{conv_id}` | — |
| 2 | POST | `/api/v1/conversations/{id}/messages` | `{text}` | `SlotExtraction` + `RequirementCardView` | `CONV_NOT_FOUND`, `LLM_FAILED`, `LLM_INVALID_OUTPUT` |
| 3 | GET | `/api/v1/conversations/{id}/card` | — | `RequirementCardView`（含 completeness / conflicts） | `CONV_NOT_FOUND` |
| 4 | PATCH | `/api/v1/conversations/{id}/card` | `{slot, value}` | `RequirementCardView` | `SLOT_UNKNOWN`, `VALUE_INVALID` |
| 5 | POST | `/api/v1/conversations/{id}/card/confirm` | — | `{card_id, version}` | **`COMPLETENESS_TOO_LOW`**（409） |
| 6 | POST | `/api/v1/plans` | `{card_id}` | `{plan_id, task_id}`（202） | **`CARD_NOT_CONFIRMED`**（409，G4 门槛）, `CARD_NOT_FOUND` |
| 7 | GET | `/api/v1/plans/{plan_id}/status` | — | `PlanStatus` | `PLAN_NOT_FOUND` |
| 8 | GET | `/api/v1/plans/{plan_id}/versions/{v}` | — | 完整方案（含 violations / checklist / cost） | `PLAN_NOT_FOUND`, `VERSION_NOT_FOUND` |
| 9 | GET | `/api/v1/search/hotels` | query params → `HotelQuery` | `SearchResult`（含 `relaxation_hints`） | `PARAM_INVALID` |
| 10 | GET | `/api/v1/trace/{plan_id}` | — | trace 列表（漏斗数据、耗时、token） | `PLAN_NOT_FOUND` |

### 长耗时接口：提交 + 轮询（不用 SSE）

- 接口 6 立即返回 `202` + `task_id`，实际生成走 `BackgroundTasks`。
- 前端每 **2s** 调接口 7。
- `PlanStatus` 结构：

```python
class PlanStatus(BaseModel):
    plan_id: str
    task_id: str
    status: Literal["queued","searching","planning","validating","costing","done","failed"]
    progress: float                # 0..1
    replan_round: int = 0
    error: ErrorBody | None = None
```

- **可脱离前端独立验证**（手册 `[C]`）：`curl -X POST .../plans -d '{"card_id":"..."}'` 然后 `watch curl .../status`。

### 校验规则

- 全部请求体与 query params 用 Pydantic 模型约束，**内部调试接口也不返回无约束数据**（手册 `[A]`）。
- 接口 9 的 `HotelQuery`：`checkout > checkin`、`adults ≥ 1`、`child_ages` 长度必须等于 `children`。

### 无动态 Agent

M0 的流程是**固定业务流程，由代码状态机编排**（手册 `[B]`）。不存在模型动态选工具的场景，因此无工具白名单 / 预算 / 最大步数的设计需求。模型只做两件事：抽取、编排——都是单次结构化输出调用。

## 八、Prompt 设计

Prompt 独立文件放 `app/services/prompts/`（手册 `[A]`），代码通过读文件加载，便于版本追踪与 diff。

### Prompt 1 · `extract_slots.md`

| 项 | 内容 |
|---|---|
| 角色 | 高端定制旅行的需求抽取助手 |
| 输入变量 | `existing_slots`（已有需求卡 JSON）、`client_message`（客户原话） |
| 输出格式 | `SlotExtraction`（Pydantic，走 `messages.parse`） |
| 核心约束 | ① 抽不到留 null，不猜；② 标注 source；③ **饮食禁忌 / 无障碍 / 预算口径三项缺失必须生成追问**；④ 识别中文委婉表达；⑤ 追问 ≤3 且能枚举就给选项；⑥ 顾问已确认的槽位不改写不重问 |

**格式约束三件套**（手册 §九.3）：

1. **Prompt 写正反例**：
   - 正例：`"budget_basis": {"value":"total","source":"client_verbatim","confidence":0.95}`
   - 反例：❌ 不要输出 `"budget_basis": "total"`（缺 source 与 confidence）；❌ 不要把「15万左右」写成 `"约150000"`（`value` 必须是数值 150000）
2. **解析器要宽容**：`SlotValue.value` 用 `str | int | float | list[str] | None` 联合类型；加 `field_validator` 把「15万」「150,000」「15w」都归一化为 `150000`。
3. **数量约束不遵守 ≠ 解析错误**：若模型给出 4 个追问（要求 ≤3），Pydantic 的 `max_length=3` 会拒绝。**处理方式是截取前 3 个并记 warning，不重试**——这是内容瑕疵不是格式错误，重试只烧钱。

### Prompt 2 · `plan_itinerary.md`

| 项 | 内容 |
|---|---|
| 角色 | 高端定制旅行的行程编排助手 |
| 输入变量 | `requirement`（需求卡）、`candidates`（候选池**摘要**）、`constraints`（约束提示 + 上一轮违规反馈） |
| 输出格式 | `ItineraryPlan`（Pydantic） |
| 核心约束 | ① **只输出 `resource_id`，不得输出任何价格 / 名称 / 营业时间 / 容量**；② 只能用候选池内的 ID；③ 同日地理就近；④ 转场日不排高强度；⑤ 住宿优先连住；⑥ 每条目给 `reason_slots` + `reason_note`；⑦ 无法满足写入 `assumptions`，不用不合适的资源凑数 |

**候选池必须摘要化**：`candidates` 只含 `resource_id` + 关键结构化字段（城市、区、档次、容量、标签、营业日、经纬度），**不含描述文本与价格**。每类资源限 Top 12 进 prompt。

**回退重排的 prompt 变化**：第 2、3 轮把上一轮的 `blocking` violations 明确写入 `constraints`，格式为「Day 3 的 `HTL-HKN-0003` 违反 H1：房型最大入住 2 人，不足 3 人。请更换。」

### 模型输出与存储的关系

| 环节 | 负责方 | 校验 |
|---|---|---|
| 结构合规 | `client.messages.parse()` + Pydantic | 失败 → 重试 1 次 → 仍失败走统一错误 `LLM_INVALID_OUTPUT` |
| **ID 有效性** | `services/render.py` | `resource_id` 必须在本次候选池内（**防线②**）；不在则该条目标 `blocked` 并触发重排 |
| **事实字段** | `services/render.py` | 从 DB 回填，逐字段与库值比对（**防线③**） |
| **金额** | `services/cost.py` | 规则引擎计算，**模型完全不参与** |
| 落库 | `plan_version.structure` | jsonb，用 `RenderedPlan` 校验 |

## 九、验收界面

**需要**。判断依据（手册第五节）：Agent 产出是「按天行程 + 成本结构 + 违规清单」这类复杂结构化内容，靠 Swagger 的 JSON 无法判断质量；且含人工确认节点，需要「用眼睛看、亲手操作」。

- **实现方式**：单个 HTML 文件放 `app/static/index.html`，FastAPI 挂载，原生 JS + `fetch`。改造自已有原型。
- **性质**：**临时验收工具**（M0）→ 但三栏交互属于产品核心，M2 提升为正式前端纵向切片。
- **功能上限清单**（只做判断核心价值所必需的）：
  - ✅ 粘贴原话、看到槽位抽取结果与四态角标（已确认 / 系统推断 / 有冲突 / 必须确认）
  - ✅ 看到追问并回答
  - ✅ 点「确认需求卡并生成」
  - ✅ 生成进度（分阶段，非流式）
  - ✅ 按天行程展示 + ⓘ 悬停看溯源（resource_id / 更新时间 / 命中需求 / 满足约束 / 价格来源）
  - ✅ 成本结构 + 计算轨迹
  - ✅ 顶部违规提示条与待核实清单
  - ❌ 拖拽调序、锁定、版本对比、导出（M2）
- **目标终端**：桌面浏览器（1440px+）。M0 不做响应式。
- **改造点**（对照现有原型）：

| 原型位置 | 改造 |
|---|---|
| `SCRIPT` 数组（写死的对话脚本） | 删除，改为 `POST /messages` |
| `renderSlots(step)` | 入参从 step 改为真实 `SlotSet`，按 `source` 渲染角标 |
| `RESOURCES` / `DAYS` / `COST` 常量 | 改为接口返回值 |
| 生成按钮 | `POST /plans` → 轮询 `status` → 渲染进度 |
| ③ 检索过程页 | 改为 `GET /trace/{plan_id}` 的真实漏斗数据 |
| ⑥ 评测框架页 | **保持静态**（展示的是设计，不是运行时数据） |

## 十、测试要求

### 第一层：mock 自动化测试（每次开发完成必跑）

模型调用一律 mock，离线可跑。

| 文件 | 用例 | 数量 |
|---|---|---|
| `test_seed_traps.py` | PRD-v3 §4.3 的 12 类约束陷阱逐条断言（T1–T12） | 12 |
| `test_constraints.py` | **7 条硬约束 × (PASS / FAIL / UNKNOWN)** | **21** |
| `test_search_filter.py` | T1（`max_occupancy=2`）/ T2（`min_child_age=7`）/ T8（合约过期）资源**不得进入候选** | 3 |
| `test_search_filter.py` | 必然无解的 query → 返回 ≥2 条放宽建议，不返回空 | 1 |
| `test_cost.py` | 3 组手工算例（含旺季上浮、儿童半价、服务费）逐分比对 | 3 |
| `test_render_guard.py` | 塞入候选池外的假 `resource_id` → 必须被防线②拦截 | 1 |
| `test_render_guard.py` | 回填后字段与库值不一致 → 必须被防线③拦截 | 1 |
| `test_conflicts.py` | 3 条冲突规则各 1 个命中用例 | 3 |
| `test_api_gate.py` | 未 confirm 的 card 调 `POST /plans` → 409 | 1 |
| `test_api_gate.py` | 完整度不足调 confirm → 409 | 1 |
| `test_tasks.py` | 僵死任务的启动补偿逻辑 | 1 |

**重点是纯函数单测**：约束校验器的三态判定与成本引擎是确定性代码，必须 100% 靠单测保证，不依赖模型。

**21 个约束单测的 UNKNOWN 部分是本阶段的技术核心**——它是唯一能证明「空值没有被当作通过」的东西。

### 第二层：真实模型端到端冒烟（阶段验收前必做）

M0 有**两种不同的模型调用契约**，各需至少一条真实链路：

| # | 契约 | 真实输入 | 跑通链路 | 记录指标 |
|---|---|---|---|---|
| 1 | 槽位抽取 | 「张女士一家，两大一小，孩子 5 岁。想 10 月中旬去日本，7 天左右。住好一点的酒店，预算 15 万左右。老人肠胃不好这次不去。」 | 真实输入 → 真实模型 → 需求卡落库 → 界面可见 | 模型 / 耗时 / 输入输出 token / 成本 / 抽出槽位数 / **结构合规（首测即合规 or 重试后合规）** / **是否触发 dietary 追问** |
| 2 | 行程编排 | 上述需求卡 confirm 后生成 | 需求卡 → 检索 → 编排 → 校验 → 核算 → 方案落库 → 界面可见 | 模型 / 耗时（首字 + 整体）/ token / 成本 / **回退轮数** / **blocking 违规数** / **引用校验拦截数** / **UNKNOWN 项数** |

**无 Key 时**：明确声明该项待验，**不得用 mock 结果冒充**（手册第十节）。

**质量瑕疵 vs 解析错误的区分**（手册 §九.3）：
- 模型给 4 个追问而非 ≤3 → **质量瑕疵**，截取前 3 并记 warning，交产品经理判断
- 模型输出的 `reason_note` 里夹了一个价格数字 → **质量瑕疵**，记录并在 prompt 里加反例，不重试
- `resource_id` 不在候选池 → **必须拦截**并重排（这是正确性问题不是瑕疵）
- Pydantic 校验失败 → 重试 1 次 → 仍失败走统一错误

### 其他必测路径

- **数据恢复**：生成中途 `kill` uvicorn → 重启 → 该任务应被标 `failed` 且带 `ORPHANED` 错误码，前端停止轮询并提示重新生成
- **错误路径**：无效 Key / 超时 / 模型返回不合规结构，三种情况都应返回统一错误结构，不泄漏堆栈
- **关键交互**：需求卡四态角标显示正确；ⓘ 浮层数据来自真实 `source` 字段

## 十一、验收清单

> 产品经理照着点即可，不需要懂技术。每项打勾或截图反馈问题。

**环境与数据**

- [ ] 执行 `docker compose up -d`，无报错
- [ ] 执行 `alembic upgrade head`，无报错
- [ ] 执行 `python seed/generate.py`，最后一行显示 **「约束陷阱校验：12/12 通过」**
- [ ] 执行 `python seed/load.py`，显示各表入库条数（酒店应为 42）
- [ ] 执行 `uvicorn app.main:app --port 8000`，浏览器打开 `http://localhost:8000` 能看到三栏界面

**需求采集**

- [ ] 粘贴一段客户原话（可用第十节的示例），点发送
- [ ] 需求卡右侧出现多个槽位，每项带角标（✓ 客户原话 / ◐ 系统推断）
- [ ] AI 提出的追问**不超过 3 个**
- [ ] 追问中**包含饮食禁忌**（因为原话说了「肠胃不好」）
- [ ] 「住好一点的」这类模糊表达，AI **给出选项**而不是问开放题
- [ ] 手动改一个槽位的值，角标变成「顾问填写」，再发一条消息时该值**不被改写**
- [ ] 完整度不足 85% 时，生成按钮为**禁用态**且说明原因

**生成方案**

- [ ] 回答追问后，完整度上升，生成按钮变为可点
- [ ] 点「确认需求卡并生成方案」，出现分阶段进度（检索中 → 编排中 → 校验中 → 核算中）
- [ ] 生成完成后能看到 7 天完整行程，**没有空白天**
- [ ] 每一天都有住宿条目（最后一天除外）
- [ ] 鼠标悬停任意条目的 ⓘ，能看到资源 ID、数据更新时间、命中需求、满足约束、价格来源
- [ ] 顶部提示条显示「硬约束 0 项违反」

**成本**

- [ ] 能看到成本结构（住宿 / 交通 / 餐饮 / 门票 / 服务费）与地面总计
- [ ] 点住宿明细，能看到「单价 × 晚数 × 季节系数」的计算轨迹
- [ ] **拿计算器手工算一遍其中一家酒店的住宿费，与界面数字一致**

**约束与空值（本阶段核心）**

- [ ] 生成的方案里**没有**「最大入住 2 人」的房型（种子数据里有 5 家这样的酒店）
- [ ] 生成的方案里**没有**周一安排周一休馆的博物馆
- [ ] 如果方案里有休馆日未记录的景点，它应出现在**待核实清单**里，而不是被当成全年无休
- [ ] 待核实清单里的每一项都写清了「要核实什么」

**人工卡点**

- [ ] 不点「确认需求卡」直接调生成接口（或刷新后直接点生成），应被拒绝并提示需求卡未确认

**容错**

- [ ] 生成过程中关掉后端再重启，刷新页面后该任务显示「已失败，请重新生成」，**不会一直转圈**
- [ ] 把 `.env` 里的 `ANTHROPIC_API_KEY` 改成错误值，发消息应看到友好的错误提示，**不出现代码报错堆栈**

**评测**

- [ ] 执行 `python eval/run.py`，输出报告
- [ ] 报告中「硬约束陷阱」族用例**通过率 100%**
- [ ] 报告中 blocking 违规总数为 **0**

## 十二、风险与待确认项

### 已知限制

| 限制 | 说明 |
|---|---|
| 资源数据为模拟数据 | 酒店名、价格、房态均为生成的种子数据。所有对外展示需标注；`is_synthetic` 字段用于将来区分 |
| 无语义检索 | 「安静有格调的温泉旅馆」这类模糊表达在 M0 靠标签匹配近似处理，效果有限。M1 补 |
| 通勤时长为估算 | haversine 直线距离 × 1.35 ÷ 城市均速。与真实路径有偏差，H5 判定存在误差 |
| 无编辑能力 | 生成结果不可改。顾问不满意只能重新生成。M2 补 |
| 单用户 | 无登录、无净价角色过滤（PRD-v2 的 G5 门槛在 M0 不适用） |
| 汇率固定 | 用 config 里的静态值，不接汇率服务 |

### 成本

单次完整生成粗估 **$0.15–0.25**（抽取 2–3 次 + 编排 1 + 回退平均 0.4 次）。开发期每天 50 次约 $10。

**省钱措施**：① system prompt 开 `cache_control`，用 `usage.cache_read_input_tokens` 验证命中；② 候选池摘要化控制输入 token；③ `eval/run.py --dry-run` 跳过 LLM 只测规则层——规则层迭代不该花模型钱。

### 安全与迁移风险

| 风险 | 缓解 |
|---|---|
| 种子数据质量不足导致硬过滤静默失效 | `assert_traps()` 作为入库前门禁；第十一节验收清单有专项检查 |
| 3 轮回退仍跑不出合法方案 | 违规明确反馈进下一轮 prompt；仍失败则**带违规交人工，不静默通过** |
| 候选池过小导致模型无从选择 | 硬过滤后候选 < 3 时先返回放宽建议给顾问，不硬生成 |
| Opus 5 参数报 400 | 不传 `temperature` / `top_p` / `budget_tokens`；用 `effort` |
| M1 迁移 | 已用 `pgvector/pgvector:pg16` 镜像，加扩展与列即可，无需换库 |

### 必须由产品经理决定的问题

见技术适配声明第 6 节的 3 个问题（目的地范围、成本上限、是否需要多用户）。**其中问题 1 会直接影响 M0 工期**，建议优先答复。

## 十三、交接给下一阶段

M0 结束时以下内容已就绪，M1 直接复用：

| 已就绪 | M1 如何复用 |
|---|---|
| PostgreSQL schema + 种子数据 | 直接加 `embed_text` / `embedding` 列与 HNSW 索引，不改表结构 |
| `services/retrieval.py` 的硬过滤层 | 保留为第①层，在其输出的候选集上追加向量与 BM25 双路召回 |
| `schemas/` 全部 Pydantic 模型 | 不变。`HotelCandidate` 增加 `semantic_score` 字段 |
| `services/constraints.py` 7 条硬约束 | 不变，M1 补到 11 条 |
| `services/cost.py` | 不变，M1 补汇率与儿童分段价 |
| `services/render.py` 三道防线 | 不变（防线②的候选池来源从「硬过滤结果」变为「混合检索结果」） |
| `generation_task` 状态机 | 增加 `embedding` 阶段 |
| 20 条评测用例 + 跑测脚本 | 直接复用，M1 补到 40 条并增加语义召回用例 |
| `app/static/index.html` | M1 继续用；M2 提升为 Next.js 时作为交互参考 |

**M1 的第一件事**：Embedding 模型选型实测——用 10 条含日文酒店名的 query 测 Recall@10，对比多语言模型与中文单语模型。**不要跳过这一步直接选型**（PRD-v2 §12.5）。

---

## 附：本文档与手册的对应关系

| 手册章节 | 本文档落点 |
|---|---|
| 二、与 PRD 的配合契约 | PRD-v2 与 PRD-v3 已覆盖 7 个产品问题；缺项在技术适配声明第 6 节列出 |
| 三、默认后端技术基线与按需模块 | 技术适配声明第 2、3 节 |
| 四、前端终端与介入时机 | 技术适配声明第 1 节（纵向切片）+ 第 4 节暂缓 1 |
| 五、正确的 Vibe Coding 流程 + 最小验收界面 | 第九节 |
| 六、技术适配声明格式 | 第一部分（按固定格式 1–6 节） |
| 七、统一环境和依赖 | 第三、四节 |
| 八、默认项目结构 | 第五节（含与 PRD-v3 的映射修正） |
| 九、工程底线 `[A]/[B]/[C]` | 贯穿；`[A]` 项集中在技术适配声明第 5 节强制底线检查 |
| 十、双层验收体系 | 第十节 |
| 第三部分 阶段文档模板 | 第二部分（十三节全覆盖） |

---

## 附录 B · 模型提供方切换：DeepSeek（2026-09-07 追加）

产品经理要求接入 DeepSeek。在不改业务代码的前提下，`core/llm.py` 增加提供方分发：

| 项 | anthropic（默认） | deepseek |
|---|---|---|
| 端点 | api.anthropic.com | `https://api.deepseek.com/anthropic`（Anthropic 兼容端点，复用同一 `anthropic` SDK） |
| 模型 | `claude-opus-5` | `deepseek-v4-pro`（或 `deepseek-v4-flash`） |
| 结构化输出 | `messages.parse(output_format=Pydantic)` | 该端点**不支持** `output_format` → 强制调用唯一工具 `emit_result`，`input_schema` = Pydantic JSON Schema，再 `model_validate` |
| 校验失败 | 重试 1 次 | 重试 1 次，且把 Pydantic 错误写回第二次请求的 `<format_errors>` |
| 思考深度 | `thinking=adaptive` + `output_config.effort` | 思考默认开启，`output_config.effort` 映射 low→low、medium/high/xhigh→high、max→max；`DEEPSEEK_THINKING=false` 可关 |
| Prompt 缓存 | `cache_control` 生效，看 `cache_read_input_tokens` | `cache_control` 被忽略（DeepSeek 自带上下文缓存，计费侧体现） |
| 金额 | 仍由 `cost.py` 计算，模型 schema 里没有金额字段 | 同 |

三道防线（schema 无事实字段 / 引用校验 / 事实回查）与约束校验器不依赖提供方，不受影响。切换后必须重跑第二层真实冒烟并记录结构合规率——DeepSeek 对 schema 的遵循度未经实测，是当前最大的未知项。

### 附录 B-2 · DeepSeek 真实模型验收记录（2026-09-07）

| 项 | 结果 |
|---|---|
| 提供方 / 模型 | deepseek / `deepseek-v4-pro`，思考关闭，强制工具调用，流式接收 |
| 契约 1 槽位抽取 | 首试结构合规；耗时约 5 s；追问 3 条含选项；饮食暗示由 `ensure_must_ask` 确定性兜底置顶 |
| 契约 2 行程编排 | 1 轮通过、0 blocking、0 引用拦截、7 天完整；耗时约 20 s |
| 20 条评测（最终） | **19/20**；硬约束陷阱族 8/8、空值族 4/4、冲突族 3/3、抽取族 4/5；**blocking 违规总数 0**（G1 达成） |
| 未通过项 | `extract_euphemism_17`：「王总夫妇带父母，四位」偶发抽成 2 成人（模型波动，非结构问题） |

**为 DeepSeek 追加的确定性防护**（均不改变模型输出边界，schema 仍无事实字段）：

1. `unwrap_tool_input`：解包 `{"result": {...}}` 类包装（含 JSON 字符串内层）。
2. `schemas/plan.py` 别名归一化：`transport→vehicle`、`attraction→poi` 等；未知值仍拒绝。
3. `_stream_with_raw_json` + `_loads_lenient`：端点约 30% 概率把工具参数返回成 `{}`，改为流式攒原始 JSON 增量并宽容解析（去尾逗号、补括号）。
4. `DEEPSEEK_MAX_ATTEMPTS=3`：结构不合规的总尝试次数。
5. `build_day_skeleton`：逐日给出日期与星期，消除 H3「当日休息」违规的主因。
6. `repair_blocking`：3 轮回退后仍有 H1/H2/H3/H4/H5/REF 违规 → 同城同类型确定性替换，记录进 assumptions；换不到才交人工。
7. `ensure_must_ask`：必问三项与饮食暗示的追问不靠模型自觉。

评测语义修正：空值族用例在 LLM 模式下判「用到的空值/非合约价资源必须在待核实清单」，模型未选到陷阱资源不算失败（原判定 `min_unknown` 只适用于确定性编排器）。
